# Topology: backend — audio storage and the HTTP storage service, nothing else.
#
# The microphone and TTS stay off (base.config leaves both topics empty), so
# this runs StorageContainer's half of BackendContainer: the storage that
# cltl-vad and cltl-asr read their audio back from.

[cltl.backend.mic]
topic:
