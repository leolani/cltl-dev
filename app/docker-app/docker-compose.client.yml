services:
  eliza-backend:
    image: ghcr.io/leolani/cltl-backend:${VERSION:-latest}
    container_name: eliza-client-backend
    ports:
      - "9001:8000"
    volumes:
      - ./config-client:/cltl-backend/config
      - ./storage-client:/cltl-backend/storage
    environment:
      - CLTL_LOGGING_CONFIG=/cltl-backend/config/logging.config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      start_period: 30s
      retries: 3
    networks:
      - eliza-client-network

  eliza-context:
    image: ghcr.io/leolani/cltl-context:${VERSION:-latest}
    container_name: eliza-client-context
    volumes:
      - ./config-client:/cltl-context/config
      - ./storage-client:/cltl-context/storage
    environment:
      - CLTL_LOGGING_CONFIG=/cltl-context/config/logging.config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      eliza-backend:
        condition: service_healthy
    networks:
      - eliza-client-network

  eliza-chatui:
    image: ghcr.io/leolani/cltl-chat-ui:${VERSION:-latest}
    container_name: eliza-client-chatui
    ports:
      - "8003:8000"
    volumes:
      - ./config-client:/cltl-chatui/config
      - ./storage-client:/cltl-chatui/storage
    environment:
      - CLTL_LOGGING_CONFIG=/cltl-chatui/config/logging.config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      eliza-backend:
        condition: service_healthy
    networks:
      - eliza-client-network

  eliza-app:
    image: ghcr.io/leolani/eliza-app:${VERSION:-latest}
    container_name: eliza-client-app
    volumes:
      - ./config-client:/app/config
      - ./storage-client:/app/storage
    environment:
      - CLTL_LOGGING_CONFIG=/app/config/logging.config
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      eliza-backend:
        condition: service_healthy
      eliza-context:
        condition: service_started
      eliza-chatui:
        condition: service_started
    networks:
      - eliza-client-network

networks:
  eliza-client-network:
    driver: bridge
