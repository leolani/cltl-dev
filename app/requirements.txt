.

cltl.backend[impl]
cltl.context[service]
cltl.asr[impl,whisper,service]
cltl.vad[impl,service]
cltl.emissor-data[service,client]
cltl.chat-ui
cltl.eliza

# TODO Fix backend optional dependencies
# gTTS: cltl_service.backend.backend_container imports cltl.backend.source.local_tts
# at module level, which imports gtts unconditionally. gTTS lives in the
# cltl.backend 'local'/'remote' extras, not 'impl', so it must be listed here.
gTTS
soundfile
cachetools
pyaudio
opencv-python==4.8.0.76
pyobjc==9.2;sys_platform=='darwin'

flask
werkzeug
